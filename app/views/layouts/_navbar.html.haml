- if !@user.nil?
	- if User.superadmin(@user.id)
		%li= link_to "Доходы и Расходы", reports_path, class: current_page?(reports_path) && 'active'
		%li= link_to "Доходы БC", statistic_path, class: current_page?(statistic_path) && 'active'
		%li= link_to "Балансы", balances_path, class: current_page?(balances_path) && 'active'
		%li= link_to "Быстрая заявка", notes_path, class: current_page?(notes_path) && 'active'
		%li= link_to "Заявки на включение", request_path, class: current_page?(request_path) && 'active'
		%li= link_to "Задачи", tasks_path, class: current_page?(tasks_path) && 'active'
		%li= link_to "Платежи", agent_payments_path, class: current_page?(agent_payments_path) && 'active'
	- if User.member(@user.id)
		%li= link_to "Балансы", balances_path, class: current_page?(balances_path) && 'active'
		%li= link_to "Заявки на включение", request_path, class: current_page?(request_path) && 'active'
		%li= link_to "Задачи", tasks_path, class: current_page?(tasks_path) && 'active'
	- if User.acceptor_payments(@user.id)
		%li= link_to "Балансы", balances_path, class: current_page?(balances_path) && 'active'
		%li= link_to "Задачи", tasks_path, class: current_page?(tasks_path) && 'active'
		%li= link_to "Платежи", agent_payments_path, class: current_page?(agent_payments_path) && 'active'
%ul.nav.pull-right
	- if @user.nil?
		Вы не вошли
		= link_to "Войти", new_session_path
	- else
		.btn-group
			= link_to "#", class: "btn btn-primary" do
				%i.icon-user.icon-white
				= @user.login
			= link_to "#", class: "btn btn-primary dropdown-toggle", "data-toggle" => "dropdown" do
				%span.caret
			%ul.dropdown-menu
				%li
					= link_to logout_path do
						%i.icon-trash
						Выйти
		